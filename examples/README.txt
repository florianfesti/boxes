Folder for autogenerated examples. Used as reference for tests.

Reference SVGs are now stored next to each generator source file
(same stem, .svg extension), not in this folder.

To regenerate a reference SVG, run:

    python scripts/regen_svg.py GeneratorName

To regenerate all reference SVGs at once:

    python scripts/regen_svg.py --all
